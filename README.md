# Luci-Memory

Run the app:

uvicorn app:app --reload

Meaning:

first app = filename app.py

second app = variable app = FastAPI()

--reload = restart automatically when code changes

You should see it start on:

http://127.0.0.1:8000